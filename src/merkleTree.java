
public class merkleTree {

	public node root; //root node
	
	public merkleTree(node hashNodes[]) { //constructor for the merkleTree
		
	}
	
	public void printTree(node pointer, int x) { //print the merkleTree
		
	}
	
	public void deleteTree(node pointer) { //delete the merkleTree
		
	}
}
